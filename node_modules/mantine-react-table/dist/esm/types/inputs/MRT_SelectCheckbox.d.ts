import { type MRT_RowData, type MRT_Row, type MRT_TableInstance } from '../types';
interface Props<TData extends MRT_RowData> {
    row?: MRT_Row<TData>;
    selectAll?: boolean;
    staticRowIndex?: number;
    table: MRT_TableInstance<TData>;
}
export declare const MRT_SelectCheckbox: <TData extends MRT_RowData>({ row, selectAll, table, staticRowIndex, }: Props<TData>) => import("react/jsx-runtime").JSX.Element;
export {};
