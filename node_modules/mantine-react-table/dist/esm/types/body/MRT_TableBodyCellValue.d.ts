/// <reference types="react" />
import { type MRT_RowData, type MRT_Cell, type MRT_TableInstance, type MRT_CellValue } from '../types';
interface Props<TData extends MRT_RowData, TValue = MRT_CellValue> {
    cell: MRT_Cell<TData, TValue>;
    table: MRT_TableInstance<TData>;
}
export declare const MRT_TableBodyCellValue: <TData extends MRT_RowData>({ cell, table, }: Props<TData, unknown>) => import("react").ReactNode;
export {};
