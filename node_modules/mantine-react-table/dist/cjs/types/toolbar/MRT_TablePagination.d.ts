import { type MRT_RowData, type MRT_TableInstance } from '../types';
interface Props<TData extends MRT_RowData> {
    position?: 'top' | 'bottom';
    table: MRT_TableInstance<TData>;
}
export declare const MRT_TablePagination: <TData extends MRT_RowData>({ table, position, }: Props<TData>) => import("react/jsx-runtime").JSX.Element;
export {};
