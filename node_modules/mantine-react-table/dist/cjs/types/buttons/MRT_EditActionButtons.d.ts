import { type MRT_RowData, type MRT_Row, type MRT_TableInstance } from '../types';
interface Props<TData extends MRT_RowData> {
    row: MRT_Row<TData>;
    table: MRT_TableInstance<TData>;
    variant?: 'icon' | 'text';
}
export declare const MRT_EditActionButtons: <TData extends MRT_RowData>({ row, table, variant, }: Props<TData>) => import("react/jsx-runtime").JSX.Element;
export {};
