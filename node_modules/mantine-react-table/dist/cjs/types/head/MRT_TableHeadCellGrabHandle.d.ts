import { type RefObject } from 'react';
import { type MRT_RowData, type MRT_Column, type MRT_TableInstance, type MRT_CellValue } from '../types';
interface Props<TData extends MRT_RowData, TValue = MRT_CellValue> {
    column: MRT_Column<TData, TValue>;
    table: MRT_TableInstance<TData>;
    tableHeadCellRef: RefObject<HTMLTableCellElement>;
}
export declare const MRT_TableHeadCellGrabHandle: <TData extends MRT_RowData>({ column, table, tableHeadCellRef, }: Props<TData, unknown>) => import("react/jsx-runtime").JSX.Element;
export {};
